
repositories{
    flatDir {
        dirs 'libs'
    }
}
android {
    sourceSets {
        main {
            jniLibs.srcDirs = ['libs']
        }
    }        
}
dependencies {
    compile(name: 'openDefault-4.4.0', ext: 'aar')
    //compile(name: 'weibo_so', ext: 'jar')
}
